name: Runner Probe

on:
  push:
    paths:
      - ".github/workflows/runner-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm hosted runner execution
        run: |
          echo "runner-online"
          python3 --version
